# Configuração de Ambiente do Nebulosa (BDSM Test)

# ── AMBIENTE E PORTA ──
PORT=3001
NODE_ENV=development

# ── BANCO DE DADOS POSTGRESQL (PRODUÇÃO) ──
# Exemplo local (substitua com sua connection string do Render em produção):
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/nebulosa

# ── SEGURANÇA ──
JWT_SECRET=super_segredo_jwt_nebulosa_v4_2026
